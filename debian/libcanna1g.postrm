#!/bin/sh

set -e

case "$1" in
    purge)
	rm -f /etc/canna/default.canna
	rm -fr /etc/canna
	;;
    
    remove)
	ldconfig
	;;
    upgrade|failed-upgrade|abort-install|abort-upgrade|disappear)
	;;
    
    *)
    ;;
esac

#DEBHELPER#
